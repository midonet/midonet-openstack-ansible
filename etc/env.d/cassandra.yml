---

component_skel:
  cassandra:
    belongs_to:
      - cassandra_all

container_skel:
  cassandra_container:
    belongs_to:
      - infra_containers
      - shared-infra_containers
    contains:
      - cassandra
    properties:
      service_name: cassandra
      container_release: trusty
